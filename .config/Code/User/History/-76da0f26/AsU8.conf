background {
    path = /home/abhinav/Desktop/wallpaper/w2.jpg
    blur_passes = 3
    blur_size = 8
}

input-field {
    monitor = eDP-1  # Change if needed
    size = 300, 80
    position = 0, 0
    valign = center
    halign = center

    outline_thickness = 2
    outer_color = rgba(200, 200, 200, 0.6)
    inner_color = rgba(50, 50, 50, 0.8)  # Increased opacity
    font_color = rgba(255, 255, 255, 1.0)

    placeholder_text = <Enter Password>  # This makes it visible before typing
    placeholder_text_color = rgba(255, 255, 255, 0.6)  # Optional
    fade_on_empty = false  # <- This line is important
}
